# Force npm to use local package instead of published version
# This prevents npm from trying to fetch @fabric-msft/fabric-angular from the registry
save-exact=true
package-lock-only=false

# Alternative: You can also use npm link for development
# npm link ../path-to-your-local-package
